{
  "name": "react-sticky-left",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "0.9.5",
    "rxjs": "^5.5.5"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0"
  },
  "homepage": "http://cecton.github.io/react-sticky-left",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
